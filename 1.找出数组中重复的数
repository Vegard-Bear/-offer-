   [题目链接](https://www.acwing.com/problem/content/14/)
   
   题目描述：
   给定一个长度为 n 的整数数组 nums，数组中所有的数字都在 0∼n−1 的范围内。数组中某些数字是重复的，但不知道有几个数字重复了，也不知道每个数字重复了几次。请找出数组中任意一个重复的数字。

注意：如果某些数字不在 0∼n−1 的范围内，或数组中不包含重复数字，则返回 -1；**rtg**

样例

   给定 nums = [2, 3, 5, 4, 3, 2, 6, 7]。
    
   返回 2 或 3。


解析：

这道题主要有三种解题思路：

   1.采用sort（）库函数进行排序后遍历一遍数组，找到第一个重复的数。
   时间复杂度O（n2），空间复杂度O（1）

   2.创建一个哈希表**vector<int>v(n)** 用于记录整个数组中某个数出现的次数，先遍历一遍数组，生成这个哈希表，然后第二遍碰到第一个值大于1的数，就输出这个下标，结束。
   时间复杂度O（n），空间复杂度O（n）

   3.（推荐）采用两两进行交换的方法。由于数组中的数字都是在0～n-1的范围内，因此如果这个数组中没有重复的数字，则每个数组排序之后下标为i的元素值应该也为i。因此，我们首先扫描这个数组，找到第一个下标为i的元素，判断它的值（假设为m）是否也为i。如果是，则继续扫描下一个元素；否则，将此元素与下标为m的元素进行比较，如果相等，则找到了第一个重复的数字，因为值为m的数出现了两次；如果不相等，则进行交换。然后再把下标为m的元素放到属于它的位置，不断重复上述过程，直到发现第一个重复的数字。
   时间复杂度O（n），空间复杂度O（1）
